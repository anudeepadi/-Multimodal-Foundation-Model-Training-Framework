"""
Test suite for the enhanced LoRA utilities contribution to HuggingFace PEFT.

This comprehensive test suite validates the functionality of the adaptive LoRA
configuration and multimodal utilities modules.
"""
import pytest\nimport torch\nimport torch.nn as nn\nfrom unittest.mock import Mock, patch\nfrom typing import List, Dict\n\nfrom adaptive_lora import AdaptiveLoRAConfig, auto_lora_config\nfrom multimodal_utils import (\n    get_vision_lora_config,\n    get_language_lora_config,\n    get_multimodal_lora_config,\n    combine_lora_configs,\n    optimize_multimodal_lora,\n    MultimodalLoRAConfig\n)\n\n\nclass MockTransformerModel(nn.Module):\n    \"\"\"Mock transformer model for testing.\"\"\"\n    \n    def __init__(self, hidden_size=768, num_layers=12):\n        super().__init__()\n        self.hidden_size = hidden_size\n        self.num_layers = num_layers\n        \n        # Create mock transformer layers\n        self.layers = nn.ModuleList([\n            nn.ModuleDict({\n                'q_proj': nn.Linear(hidden_size, hidden_size),\n                'k_proj': nn.Linear(hidden_size, hidden_size),\n                'v_proj': nn.Linear(hidden_size, hidden_size),\n                'out_proj': nn.Linear(hidden_size, hidden_size),\n                'fc1': nn.Linear(hidden_size, hidden_size * 4),\n                'fc2': nn.Linear(hidden_size * 4, hidden_size),\n            }) for _ in range(num_layers)\n        ])\n        \n        # Output projection\n        self.output_projection = nn.Linear(hidden_size, hidden_size)\n    \n    def forward(self, x):\n        return x\n\n\nclass MockCLIPModel(nn.Module):\n    \"\"\"Mock CLIP model for testing.\"\"\"\n    \n    def __init__(self):\n        super().__init__()\n        \n        # Vision encoder\n        self.vision_model = MockTransformerModel(hidden_size=768, num_layers=12)\n        \n        # Text encoder  \n        self.text_model = MockTransformerModel(hidden_size=512, num_layers=12)\n        \n        # Projection layers\n        self.visual_projection = nn.Linear(768, 512)\n        self.text_projection = nn.Linear(512, 512)\n    \n    def forward(self, **kwargs):\n        return Mock()\n\n\nclass MockLLaVAModel(nn.Module):\n    \"\"\"Mock LLaVA model for testing.\"\"\"\n    \n    def __init__(self):\n        super().__init__()\n        \n        # Vision tower\n        self.vision_tower = MockTransformerModel(hidden_size=1024, num_layers=24)\n        \n        # Language model\n        self.language_model = MockTransformerModel(hidden_size=4096, num_layers=32)\n        \n        # Multimodal projector\n        self.mm_projector = nn.Sequential(\n            nn.Linear(1024, 4096),\n            nn.GELU(),\n            nn.Linear(4096, 4096)\n        )\n\n\nclass TestAdaptiveLoRAConfig:\n    \"\"\"Test suite for AdaptiveLoRAConfig.\"\"\"\n    \n    def test_from_model_basic(self):\n        \"\"\"Test basic model analysis and configuration generation.\"\"\"\n        model = MockTransformerModel(hidden_size=768, num_layers=12)\n        \n        config = AdaptiveLoRAConfig.from_model(\n            model,\n            target_efficiency=0.95\n        )\n        \n        # Check that configuration was generated\n        assert config.recommended_rank is not None\n        assert config.recommended_alpha is not None\n        assert config.recommended_dropout is not None\n        assert config.recommended_modules is not None\n        \n        # Check reasonable values\n        assert 4 <= config.recommended_rank <= 256\n        assert config.recommended_alpha == config.recommended_rank * 2\n        assert 0.0 <= config.recommended_dropout <= 0.5\n    \n    def test_model_size_analysis(self):\n        \"\"\"Test model size analysis.\"\"\"\n        small_model = MockTransformerModel(hidden_size=256, num_layers=4)\n        large_model = MockTransformerModel(hidden_size=2048, num_layers=48)\n        \n        small_config = AdaptiveLoRAConfig.from_model(small_model)\n        large_config = AdaptiveLoRAConfig.from_model(large_model)\n        \n        # Larger models should get higher recommended ranks\n        assert large_config.recommended_rank >= small_config.recommended_rank\n        \n        # Check model analysis\n        assert small_config.total_parameters < large_config.total_parameters\n        assert small_config.model_size_mb < large_config.model_size_mb\n    \n    def test_memory_budget_constraint(self):\n        \"\"\"Test memory budget constraint application.\"\"\"\n        model = MockTransformerModel(hidden_size=1024, num_layers=24)\n        \n        # Test with tight memory budget\n        constrained_config = AdaptiveLoRAConfig.from_model(\n            model,\n            memory_budget_mb=100  # Very tight budget\n        )\n        \n        # Test with loose memory budget\n        unconstrained_config = AdaptiveLoRAConfig.from_model(\n            model,\n            memory_budget_mb=10000  # Large budget\n        )\n        \n        # Constrained config should have lower rank\n        assert constrained_config.recommended_rank <= unconstrained_config.recommended_rank\n    \n    def test_architecture_detection(self):\n        \"\"\"Test architecture type detection.\"\"\"\n        clip_model = MockCLIPModel()\n        llava_model = MockLLaVAModel()\n        \n        clip_config = AdaptiveLoRAConfig.from_model(clip_model)\n        llava_config = AdaptiveLoRAConfig.from_model(llava_model)\n        \n        # Architecture detection might not work perfectly with mocks,\n        # but ensure it doesn't crash\n        assert clip_config.architecture_type is not None\n        assert llava_config.architecture_type is not None\n    \n    def test_target_modules_detection(self):\n        \"\"\"Test automatic target modules detection.\"\"\"\n        model = MockTransformerModel()\n        config = AdaptiveLoRAConfig.from_model(model)\n        \n        # Should detect attention and MLP modules\n        expected_modules = ['q_proj', 'k_proj', 'v_proj', 'out_proj', 'fc1', 'fc2']\n        \n        # Check that some expected modules are found\n        found_modules = set(config.recommended_modules)\n        expected_set = set(expected_modules)\n        \n        assert len(found_modules.intersection(expected_set)) > 0\n    \n    def test_to_lora_config(self):\n        \"\"\"Test conversion to LoraConfig.\"\"\"\n        model = MockTransformerModel()\n        adaptive_config = AdaptiveLoRAConfig.from_model(model)\n        \n        lora_config = adaptive_config.to_lora_config()\n        \n        # Check that LoRA config has correct attributes\n        assert lora_config.r == adaptive_config.recommended_rank\n        assert lora_config.lora_alpha == adaptive_config.recommended_alpha\n        assert lora_config.lora_dropout == adaptive_config.recommended_dropout\n        assert lora_config.target_modules == adaptive_config.recommended_modules\n    \n    def test_performance_impact_estimation(self):\n        \"\"\"Test performance impact estimation.\"\"\"\n        model = MockTransformerModel()\n        config = AdaptiveLoRAConfig.from_model(model)\n        \n        impact = config.estimate_performance_impact()\n        \n        # Check that all expected keys are present\n        expected_keys = [\n            'performance_retention',\n            'memory_overhead_ratio',\n            'speed_ratio',\n            'trainable_parameters',\n            'trainable_ratio'\n        ]\n        \n        for key in expected_keys:\n            assert key in impact\n        \n        # Check reasonable value ranges\n        assert 0.8 <= impact['performance_retention'] <= 1.0\n        assert 0.0 <= impact['memory_overhead_ratio'] <= 0.1\n        assert 0.8 <= impact['speed_ratio'] <= 1.0\n        assert impact['trainable_parameters'] > 0\n    \n    def test_auto_lora_config_function(self):\n        \"\"\"Test the convenience function.\"\"\"\n        model = MockTransformerModel()\n        \n        lora_config = auto_lora_config(\n            model,\n            efficiency_target=0.9,\n            memory_budget_mb=2000\n        )\n        \n        # Should return a valid LoraConfig\n        assert hasattr(lora_config, 'r')\n        assert hasattr(lora_config, 'lora_alpha')\n        assert hasattr(lora_config, 'target_modules')\n        assert hasattr(lora_config, 'lora_dropout')\n\n\nclass TestMultimodalUtils:\n    \"\"\"Test suite for multimodal utilities.\"\"\"\n    \n    def test_get_vision_lora_config(self):\n        \"\"\"Test vision LoRA configuration generation.\"\"\"\n        # Test different architectures\n        architectures = ['clip', 'vit', 'swin', 'resnet']\n        \n        for arch in architectures:\n            config = get_vision_lora_config(architecture=arch, rank=16)\n            \n            assert config.r == 16\n            assert config.lora_alpha == 32  # 2 * rank\n            assert config.lora_dropout == 0.1\n            assert len(config.target_modules) > 0\n    \n    def test_get_language_lora_config(self):\n        \"\"\"Test language LoRA configuration generation.\"\"\"\n        # Test different architectures\n        architectures = ['bert', 'llama', 'gpt', 't5']\n        \n        for arch in architectures:\n            config = get_language_lora_config(architecture=arch, rank=16)\n            \n            assert config.r == 16\n            assert config.lora_alpha == 32  # 2 * rank\n            assert config.lora_dropout == 0.1\n            assert len(config.target_modules) > 0\n    \n    def test_get_multimodal_lora_config(self):\n        \"\"\"Test multimodal LoRA configuration generation.\"\"\"\n        model_types = ['clip', 'blip', 'llava', 'flamingo']\n        \n        for model_type in model_types:\n            config = get_multimodal_lora_config(\n                model_type=model_type,\n                vision_rank=16,\n                text_rank=16,\n                shared_rank=8\n            )\n            \n            # Check that all components are present\n            assert config.vision_config is not None\n            assert config.text_config is not None\n            assert config.shared_config is not None\n            assert config.fusion_modules is not None\n            \n            # Check configuration values\n            assert config.vision_config.r == 16\n            assert config.text_config.r == 16\n            assert config.shared_config.r == 8\n    \n    def test_combine_lora_configs(self):\n        \"\"\"Test LoRA configuration combination.\"\"\"\n        config1 = get_vision_lora_config('clip', rank=16)\n        config2 = get_language_lora_config('bert', rank=16)\n        \n        # Test union combination\n        combined_union = combine_lora_configs([config1, config2], \"union\")\n        \n        # Should have modules from both configs\n        all_modules = set(config1.target_modules + config2.target_modules)\n        combined_modules = set(combined_union.target_modules)\n        \n        assert combined_modules == all_modules\n        \n        # Test intersection combination\n        combined_intersect = combine_lora_configs([config1, config2], \"intersection\")\n        \n        # Should only have common modules (likely none for different architectures)\n        intersect_modules = set(config1.target_modules) & set(config2.target_modules)\n        assert set(combined_intersect.target_modules) == intersect_modules\n    \n    def test_optimize_multimodal_lora(self):\n        \"\"\"Test multimodal LoRA optimization.\"\"\"\n        clip_model = MockCLIPModel()\n        \n        optimized_config = optimize_multimodal_lora(\n            model=clip_model,\n            model_type=\"clip\",\n            performance_budget=0.95,\n            memory_budget_mb=4000\n        )\n        \n        # Check that optimization produced valid configuration\n        assert optimized_config.vision_config is not None\n        assert optimized_config.text_config is not None\n        \n        # Check that ranks are reasonable\n        assert 4 <= optimized_config.vision_config.r <= 256\n        assert 4 <= optimized_config.text_config.r <= 256\n    \n    def test_multimodal_config_dataclass(self):\n        \"\"\"Test MultimodalLoRAConfig dataclass.\"\"\"\n        vision_config = get_vision_lora_config('clip', rank=16)\n        text_config = get_language_lora_config('bert', rank=16)\n        \n        config = MultimodalLoRAConfig(\n            vision_config=vision_config,\n            text_config=text_config,\n            fusion_modules=[\"projection\"]\n        )\n        \n        assert config.vision_config == vision_config\n        assert config.text_config == text_config\n        assert config.fusion_modules == [\"projection\"]\n        assert config.shared_config is None\n\n\nclass TestIntegration:\n    \"\"\"Integration tests for the contribution.\"\"\"\n    \n    def test_adaptive_with_multimodal(self):\n        \"\"\"Test integration between adaptive and multimodal utilities.\"\"\"\n        clip_model = MockCLIPModel()\n        \n        # Use adaptive configuration for the full model\n        adaptive_config = AdaptiveLoRAConfig.from_model(\n            clip_model,\n            target_efficiency=0.9\n        )\n        \n        # Also get specialized multimodal configuration\n        multimodal_config = get_multimodal_lora_config(\n            \"clip\",\n            vision_rank=adaptive_config.recommended_rank,\n            text_rank=adaptive_config.recommended_rank\n        )\n        \n        # Both should produce reasonable configurations\n        assert adaptive_config.recommended_rank > 0\n        assert multimodal_config.vision_config.r > 0\n        assert multimodal_config.text_config.r > 0\n    \n    def test_end_to_end_workflow(self):\n        \"\"\"Test complete workflow from model to final config.\"\"\"\n        # Create mock model\n        model = MockCLIPModel()\n        \n        # Step 1: Analyze model and get adaptive configuration\n        adaptive_config = AdaptiveLoRAConfig.from_model(\n            model,\n            target_efficiency=0.95,\n            memory_budget_mb=3000\n        )\n        \n        # Step 2: Get performance estimates\n        impact = adaptive_config.estimate_performance_impact()\n        \n        # Step 3: Convert to standard LoRA config\n        final_config = adaptive_config.to_lora_config()\n        \n        # Verify the workflow completed successfully\n        assert final_config.r > 0\n        assert final_config.lora_alpha > 0\n        assert len(final_config.target_modules) > 0\n        assert 0.8 <= impact['performance_retention'] <= 1.0\n\n\nif __name__ == \"__main__\":\n    # Run basic tests if pytest is not available\n    print(\"Running basic tests...\")\n    \n    # Test adaptive configuration\n    test_adaptive = TestAdaptiveLoRAConfig()\n    test_adaptive.test_from_model_basic()\n    test_adaptive.test_model_size_analysis()\n    print(\"✓ AdaptiveLoRAConfig tests passed\")\n    \n    # Test multimodal utilities\n    test_multimodal = TestMultimodalUtils()\n    test_multimodal.test_get_vision_lora_config()\n    test_multimodal.test_get_language_lora_config()\n    test_multimodal.test_get_multimodal_lora_config()\n    print(\"✓ Multimodal utilities tests passed\")\n    \n    # Test integration\n    test_integration = TestIntegration()\n    test_integration.test_end_to_end_workflow()\n    print(\"✓ Integration tests passed\")\n    \n    print(\"\\n🎉 All tests passed! Contribution is ready for review.\")\n"